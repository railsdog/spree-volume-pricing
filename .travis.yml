before_script:
  - "ruby --version"
  - "bundle exec rake test_app"
script: "bundle exec rake"
notifications:
  irc:
    channels:
      - "irc.freenode.org#spree"
    use_notice: true
    skip_join: true

rvm:
  - 1.9.3
  - 2.0.0
